@namespace BlazorApp.Client.Shared
@using Microsoft.AspNetCore.Components

<div class="card responsive-youtube-card @CssClass">
    <div class="card-body">
        @if (!string.IsNullOrWhiteSpace(Title))
        {
            <h5 class="card-title">@Title</h5>
        }

        @if (!string.IsNullOrWhiteSpace(Description))
        {
            <p class="card-text">@Description</p>
        }

        @if (!string.IsNullOrWhiteSpace(YouTubeUrl))
        {
            <div class="video-container">
                <iframe src="@YouTubeUrl" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen title="@Title"></iframe>
            </div>
        }
        else
        {
            <p class="text-muted">Enlace de YouTube inválido o no proporcionado.</p>
        }
    </div>
</div>

@code {
    [Parameter]
    public string Title { get; set; } = string.Empty;

    [Parameter]
    public string Description { get; set; } = string.Empty;

    /// <summary>
    /// URL completa para usar en el iframe (puede ser un enlace de embed o cualquier URL válida).
    /// </summary>
    [Parameter]
    public string YouTubeUrl { get; set; } = string.Empty;

    /// <summary>
    /// Clases CSS adicionales para la tarjeta.
    /// </summary>
    [Parameter]
    public string CssClass { get; set; } = string.Empty;
}

<style>
.responsive-youtube-card .card-title { margin-bottom: .5rem; }
.video-container { position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden; }
.video-container iframe { position: absolute; top: 0; left: 0; width: 100%; height: 100%; border: 0; }
@@media (max-width: 576px) {
    .responsive-youtube-card .card-body { padding: .75rem; }
}
</style>
